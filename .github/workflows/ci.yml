name: CI

on:
  pull_request:
    branches: [master]
  push:
    branches: [master]

jobs:
  backend-tests:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: backend/requirements.txt

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run tests
        run: python -m pytest tests/ -v

  frontend-build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build (includes TypeScript + ESLint)
        run: npm run build

  # Deploy job temporarily disabled while debugging CI failures.
  # Re-enable after CI passes. The deploy job needs:
  # 1. heroku authorizations:create -d "GitHub Actions" -S
  # 2. Add token as HEROKU_API_KEY in GitHub repo Settings > Secrets
  # 3. Disable auto-deploy in Heroku Dashboard > Deploy > GitHub
  #
  # deploy:
  #   needs: [backend-tests, frontend-build]
  #   if: github.ref == 'refs/heads/master' && github.event_name == 'push'
  #   runs-on: ubuntu-latest
  #   concurrency:
  #     group: heroku-deploy
  #     cancel-in-progress: true
  #   steps:
  #     - uses: actions/checkout@v4
  #       with:
  #         fetch-depth: 0
  #     - name: Deploy to Heroku
  #       env:
  #         HEROKU_API_KEY: ${{ secrets.HEROKU_API_KEY }}
  #       run: |
  #         git remote add heroku https://heroku:$HEROKU_API_KEY@git.heroku.com/bainluck.git || true
  #         git push heroku master --force
