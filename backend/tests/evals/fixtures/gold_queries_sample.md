# GOLD QUERY SET — committed CI fixture (a small sample of the dev-only draft)
# This is a stable, checked-in slice of .claude/handoff/gold_queries_draft.md so the
# gold-parser harness exercises real parsing in CI instead of skipping. The full,
# evolving set stays gitignored under .claude/handoff/; keep this sample in sync
# with the parser's coverage/real-half format, not with the full query list.
Teams: red sox · celtics (team pages)
Events: the open · world cup (concept/hub pages)
## THE REAL HALF (sample of Alex's actual history — primary)
Native Recents: Golf · MLB · "Where will Taylor Swift and Travis Kelce's Wedding occur?"
Desktop Recents (autocomplete-aided): The Open Championship Winner
