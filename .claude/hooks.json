{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "cd /home/user/odds-tracker/backend && pip install -q -r requirements.txt 2>&1 | tail -1; cd /home/user/odds-tracker/frontend && npm install --silent 2>&1 | tail -1; echo 'Dependencies installed: backend (pip) + frontend (npm)'"
          }
        ]
      }
    ]
  }
}
